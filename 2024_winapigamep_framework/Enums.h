#pragma once
enum class LAYER
{
	DEFAULT,
	BACKGROUND,
	Ground,
	ENEMY,
	BOSS_ATTACK_OBJECT,
	BOSS,
	PROJECTILE,
	BOSS_PROJECTILE,
	PORTAL,
	PLAYER,
	EFFECT,
	// UI
	UI,
	END
};

enum class PEN_TYPE
{
	HOLLOW, RED, GREEN,
	BLUE, YELLOW, END
};

enum class BRUSH_TYPE
{
	HOLLOW, RED, GREEN,
	BLUE, YELLOW, END
};


enum class EVENT_TYPE
{
	CREATE_OBJECT,
	DELETE_OBJECT,
	SCENE_CHANGE,
	END,
};

enum class BOSS_STATE
{
	PAWN, BISHOP, KNIGHT, ROOK, QUEEN, KING
};

enum class PLAYER_STATE
{
	IDLE, MOVE, JUMP, DASH, DEAD
};

enum class EFFECT_TYPE {
	BOSS_DEAD, PLAYER_HIT
};